#include "main.h"

/**
  * add - add two integers and returns the result
  * @a: one of the parameters
  * @b: the other parameter
  * Return: 0
  */
int add(int a, int b)
{
	a = 90;
	b = 8;
	return (a + b);
}
