#include <stdio.h>
/**
  * main - checks the code correctness I guess
  * @argv: one of the parameters used for printing
  * @argc: the other parameter used for printing
  * Return: Always 0.
  */
int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
